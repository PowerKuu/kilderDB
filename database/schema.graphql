type Category {
    id:ID!

    name: String! @search(by: [fulltext])
    sources: [Source]! @hasInverse(field:"category")
}

type Source {
    id:ID!
    
    name: String! @search(by: [fulltext])
    content: String!

    category: Category!
}